<div class="container">
  <div class="row">
    <div class="col-md-6 mx-auto">
      <% if @notifications.where.not(visitor_id: current_user.id) %>
        <% if @notifications.exists? %>
          <%= link_to "<%= @notification.visitor.name %>さん",user_path(@notification.visitor), class: "mx-1 nav-link btn btn-light" %>
        <% else %>
        
    </div>
  </div>
</div>
